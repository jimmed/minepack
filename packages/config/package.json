{
  "name": "@minepack/config",
  "version": "0.1.0",
  "description": "API for managing minepack config directory",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf lib",
    "test": "echo 'No tests yet'"
  },
  "devDependencies": {
    "@types/node": "^12.7.8"
  },
  "engines": {
    "node": ">=12.10.0"
  }
}
